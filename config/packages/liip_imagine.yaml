# See dos how to configure the bundle: https://symfony.com/doc/current/bundles/LiipImagineBundle/basic-usage.html
liip_imagine:
    # valid drivers options include "gd" or "gmagick" or "imagick"
    driver: "gd"

    filter_sets:
        updateThumbnailsUser:
            filters:
                thumbnail:
                    size: [350, 350]
                    mode: outbound
                    allow_upscale: true